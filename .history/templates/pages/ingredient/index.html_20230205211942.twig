{% extends 'base.html.twig' %}

{% block title %}Recettes ingredients
{% endblock %}

{% block body %}
	<div class="container">
		<h1>Mes ingredients</h1>

		{% for ingredient in ingredients %}
			<div class="card" style="width: 18rem;">
				<div class="card-body">
					<h5 class="card-title">Nom de l'ingredient: {{ ingredient.name }}</h5>
					<p class="card-text">Prix: {{ ingredient.price }}</p>
					{#<a href="{{ path('ingredient', {'id': ingredient.id}) }}" class="btn btn-dark">Voir</a>#}
				</div>
			</div>
		{% endfor %}

	</div>
{% endblock %}
